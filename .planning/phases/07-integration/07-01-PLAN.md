---
phase: 07-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/auto_aim.toml
  - config/models/tiny_resnet.onnx
  - app/auto_aim/detector.cpp
autonomous: true
requirements:
  - CFG-01

must_haves:
  truths:
    - "config/auto_aim.toml exists with all six sections: [classifier] [detector] [solver] [tracker] [aimer] [shooter]"
    - "Each section's keys match the parameters read by its corresponding module constructor"
    - "[solver] array parameters have exact element counts: R_* = 9, t_* = 3, camera_matrix = 9, distort_coeffs = 5"
    - "detector.cpp line 41 cv::imshow is wrapped in if (debug_) guard"
    - "tiny_resnet.onnx is copied to config/models/ and is a valid file"
    - "classify_model path in config points to the copied model"
  artifacts:
    - path: "config/auto_aim.toml"
      provides: "Merged TOML configuration for all six auto-aim modules"
      min_lines: 70
      contains: "section headers for classifier, detector, solver, tracker, aimer, shooter"
    - path: "app/auto_aim/detector.cpp"
      provides: "Fixed imshow at line 41 guarded by debug_ flag"
      contains: "if (debug_) cv::imshow"
    - path: "config/models/tiny_resnet.onnx"
      provides: "ONNX model file for Classifier construction"
      exists: true
  key_links:
    - from: "config/auto_aim.toml"
      to: "classifier.cpp"
      via: "config[\"classifier\"][\"classify_model\"]"
      pattern: "classify_model"
    - from: "config/auto_aim.toml"
      to: "detector.cpp"
      via: "config[\"detector\"][\"threshold\"] (and 8 other keys)"
      pattern: "detector"
    - from: "config/auto_aim.toml"
      to: "solver.cpp"
      via: "config[\"solver\"][\"R_gimbal2imubody\"] (and 4 other arrays)"
      pattern: "solver"
    - from: "config/auto_aim.toml"
      to: "tracker.cpp"
      via: "config[\"tracker\"][\"enemy_color\"]"
      pattern: "tracker"
    - from: "config/auto_aim.toml"
      to: "aimer.cpp"
      via: "config[\"aimer\"][\"yaw_offset\"]"
      pattern: "aimer"
    - from: "config/auto_aim.toml"
      to: "shooter.cpp"
      via: "config[\"shooter\"][\"first_tolerance\"]"
      pattern: "shooter"
---

<objective>
Create the merged auto-aim TOML configuration file (config/auto_aim.toml) with all six module sections, fix the unconditional cv::imshow call in detector.cpp, and copy the ONNX model file for classifier tests.

Purpose: Establish the single source of truth for all auto-aim module parameters, matching each module's constructor key names and default values. Fix the headless-environment crash by guarding cv::imshow behind the existing debug_ member. Provide the ONNX model required for Classifier/Detector construction so downstream tests can execute the full pipeline.

Output: config/auto_aim.toml (70+ lines, 6 sections), modified app/auto_aim/detector.cpp (1-line change), config/models/tiny_resnet.onnx (~1MB binary copy).
</objective>

<execution_context>
@/home/eldwen/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-integration/07-RESEARCH.md

<interfaces>
Key types consumed by downstream plans (Plan 02 test executable):

From app/auto_aim/classifier.cpp (constructor):
```cpp
Classifier::Classifier(const std::string & config_path) {
  auto config = toml::parse_file(config_path);
  auto model = config["classifier"]["classify_model"].value_or<std::string>("");
  net_ = cv::dnn::readNetFromONNX(model);
}
```

From app/auto_aim/detector.cpp (config keys, lines 19-28):
```
threshold_: 150.0, max_angle_error_: 15.0/57.3, min_lightbar_ratio_: 1.5, max_lightbar_ratio_: 10.0
min_lightbar_length_: 10.0, min_armor_ratio_: 1.0, max_armor_ratio_: 4.0
max_side_ratio_: 2.0, min_confidence_: 0.5, max_rectangular_error_: 20.0/57.3
```

From app/auto_aim/solver.cpp (config keys, lines 46-65):
```
R_gimbal2imubody: 3x3 array (9 elems), R_camera2gimbal: 3x3 array (9 elems)
t_camera2gimbal: 3-element array
camera_matrix: 3x3 array (9 elems), distort_coeffs: 5-element array
yaw_search_range: 140.0 (scalar)
```

From app/auto_aim/tracker.cpp (config keys, lines 19-24):
```
enemy_color: string ("red"/"blue"), min_detect_count: 5, max_temp_lost_count: 50
```

From app/auto_aim/aimer.cpp (config keys, lines 18-33):
```
yaw_offset, pitch_offset, comming_angle, leaving_angle: doubles, /57.3
high_speed_delay_time, low_speed_delay_time, decision_speed: doubles
left_yaw_offset, right_yaw_offset: optional doubles, /57.3
```

From app/auto_aim/shooter.cpp (config keys, lines 13-17):
```
first_tolerance, second_tolerance: doubles, /57.3
judge_distance: double, auto_fire: bool
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config/auto_aim.toml with all six module sections</name>
  <files>config/auto_aim.toml</files>
  <action>
    Create config/auto_aim.toml with six TOML sections. Use the existing testconfig.toml style (spaces around =, ### comments for section headers). Every key name must match what the corresponding module's constructor reads. Follow these specifications exactly:

    **Section [classifier]**: (from classifier.cpp line 12)
    - classify_model = "/home/eldwen/TGU_Robocore_2027/config/models/tiny_resnet.onnx"

    **Section [detector]**: (from detector.cpp lines 19-28, production defaults)
    - threshold = 150.0
    - max_angle_error = 15.0
    - min_lightbar_ratio = 1.5
    - max_lightbar_ratio = 10.0
    - min_lightbar_length = 10.0
    - min_armor_ratio = 1.0
    - max_armor_ratio = 4.0
    - max_side_ratio = 2.0
    - min_confidence = 0.5
    - max_rectangular_error = 20.0

    **Section [solver]**: (from solver.cpp lines 46-65)
    - R_gimbal2imubody = [1, 0, 0, 0, 1, 0, 0, 0, 1]  (identity, 9 elements)
    - R_camera2gimbal = [1, 0, 0, 0, 1, 0, 0, 0, 1]  (identity, 9 elements)
    - t_camera2gimbal = [0, 0, 0]  (3 elements)
    - camera_matrix = [1000, 0, 320, 0, 1000, 240, 0, 0, 1]  (9 elements, matching 640x480 test image)
    - distort_coeffs = [0, 0, 0, 0, 0]  (5 elements, zero distortion)
    - yaw_search_range = 140

    **Section [tracker]**: (from tracker.cpp lines 19-24)
    - enemy_color = "blue"
    - min_detect_count = 5
    - max_temp_lost_count = 50

    **Section [aimer]**: (from aimer.cpp lines 18-33)
    - yaw_offset = 0.0
    - pitch_offset = 0.0
    - comming_angle = 0.0
    - leaving_angle = 0.0
    - high_speed_delay_time = 0.0
    - low_speed_delay_time = 0.0
    - decision_speed = 0.0
    - (left_yaw_offset and right_yaw_offset are OPTIONAL -- omit them as comments, matching the aimer.cpp logic that checks for node existence)

    **Section [shooter]**: (from shooter.cpp lines 13-17)
    - first_tolerance = 0.0
    - second_tolerance = 0.0
    - judge_distance = 0.0
    - auto_fire = false

    **Format rules:**
    - Use `key = value` with single space around =
    - String values in double quotes
    - Arrays use square brackets with comma-space separation
    - Comments start with #
    - Section headers: [section_name]
    - Add a header comment block at the top: "# Auto-aim Configuration"
    - Add a blank line between sections for readability
  </action>
  <verify>
    <automated>
      cd /home/eldwen/TGU_Robocore_2027 && \
      python3 -c "
import tomllib
with open('config/auto_aim.toml', 'rb') as f:
    c = tomllib.load(f)
assert 'classifier' in c, 'Missing [classifier]'
assert 'detector' in c, 'Missing [detector]'
assert 'solver' in c, 'Missing [solver]'
assert 'tracker' in c, 'Missing [tracker]'
assert 'aimer' in c, 'Missing [aimer]'
assert 'shooter' in c, 'Missing [shooter]'
assert len(c['solver']['R_gimbal2imubody']) == 9
assert len(c['solver']['R_camera2gimbal']) == 9
assert len(c['solver']['t_camera2gimbal']) == 3
assert len(c['solver']['camera_matrix']) == 9
assert len(c['solver']['distort_coeffs']) == 5
assert len(c['detector']) == 10, f'detector has {len(c[\"detector\"])} keys, expected 10'
assert c['shooter']['auto_fire'] == False
print('All config validations passed')
      "
    </automated>
  </verify>
  <done>
    config/auto_aim.toml exists with 6 sections, all key names match constructor reads, solver arrays have exact element counts, auto_fire is false
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix detector.cpp unconditional cv::imshow and copy ONNX model</name>
  <files>app/auto_aim/detector.cpp, config/models/tiny_resnet.onnx</files>
  <action>
    Apply two independent changes:

    1. **Fix imshow guard**: In app/auto_aim/detector.cpp line 41, change the unconditional `cv::imshow("binary_img", binary_img);` to `if (debug_) cv::imshow("binary_img", binary_img);`. The debug_ member already exists and is used at line 109 to guard show_result(). This is a 1-line change. Do not modify any other part of detector.cpp.

    2. **Copy ONNX model**: Copy the ONNX model file from sp_vision_25 to config/models/:
       ```bash
       cp /home/eldwen/sp_vision_25/assets/tiny_resnet.onnx /home/eldwen/TGU_Robocore_2027/config/models/tiny_resnet.onnx
       ```
       The config/models/ directory already exists. The file is approximately 1MB.
  </action>
  <verify>
    <automated>
      cd /home/eldwen/TGU_Robocore_2027 && \
      grep -n 'cv::imshow' app/auto_aim/detector.cpp | grep -q 'if (debug_)' && echo "imshow guard: PASS" || echo "imshow guard: FAIL" && \
      stat config/models/tiny_resnet.onnx >/dev/null 2>&1 && echo "model file exists: PASS" || echo "model file exists: FAIL" && \
      file config/models/tiny_resnet.onnx | grep -q "Open Neural Network" && echo "model type: PASS" || echo "model type: FAIL" && \
      actual_count=$(grep -c 'if (debug_) cv::imshow' app/auto_aim/detector.cpp) && \
      test "$actual_count" -eq 1 && echo "single guard: PASS" || echo "single guard: FAIL (count=$actual_count)"
    </automated>
  </verify>
  <done>
    detector.cpp has exactly one if (debug_) cv::imshow call at line 41. config/models/tiny_resnet.onnx exists and is a valid ONNX model file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> config file | TOML config read from disk by all modules |
| filesystem -> ONNX model | Model loaded by Classifier via cv::dnn::readNetFromONNX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T (Tampering) | config/auto_aim.toml | accept | Config is a static asset checked into version control; runtime tampering requires filesystem access already granted to the process. No PII or credentials in config. |
| T-07-02 | T (Tampering) | config/models/tiny_resnet.onnx | accept | ONNX model is a static binary asset. Tampering would require filesystem write access. Model integrity verification is out of scope for this phase. |
</threat_model>

<verification>
- [ ] tomllib.load succeeds on config/auto_aim.toml with 6 sections
- [ ] Each solver array has exact element count (9/9/3/9/5)
- [ ] detector.cpp has only guarded imshow
- [ ] tiny_resnet.onnx exists in config/models/
- [ ] No other lines in detector.cpp were modified
</verification>

<success_criteria>
- Merged config file passes TOML parsing validation
- All detector config keys match the 10 parameters read in detector.cpp constructor
- cv::imshow is guarded by debug_ flag (single occurrence)
- ONNX model is available at config/models/tiny_resnet.onnx
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration/07-01-SUMMARY.md`
</output>
