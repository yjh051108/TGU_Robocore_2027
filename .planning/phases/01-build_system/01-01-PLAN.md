---
phase: 01-build_system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [BLD-01, BLD-02]
user_setup: []

must_haves:
  truths:
    - "Build toolchain is available (cmake >= 3.16, g++ >= 10)"
    - "Eigen3 library is installed and discoverable by CMake"
    - "All project dependencies (Boost, OpenCV, Aravis, libusb) are installed"
  artifacts:
    - path: "/usr/bin/cmake"
      provides: "CMake build system"
      contains: "cmake"
    - path: "/usr/bin/g++"
      provides: "C++20 compiler"
      contains: "g++"
    - path: "/usr/include/eigen3/Eigen/Dense"
      provides: "Eigen3 linear algebra headers"
      min_lines: 1
    - path: "/usr/lib/x86_64-linux-gnu/cmake/eigen3/Eigen3Config.cmake"
      provides: "CMake config for Eigen3 find_package"
      min_lines: 1
    - path: "/usr/lib/x86_64-linux-gnu/cmake/boost*/"
      provides: "Boost CMake config files"
    - path: "/usr/lib/x86_64-linux-gnu/cmake/opencv4/"
      provides: "OpenCV CMake config files"
    - path: "/usr/lib/x86_64-linux-gnu/pkgconfig/aravis-0.8.pc"
      provides: "Aravis pkg-config file"
  key_links:
    - from: "CMakeLists.txt find_package(Eigen3 REQUIRED)"
      to: "/usr/lib/x86_64-linux-gnu/cmake/eigen3/Eigen3Config.cmake"
      pattern: "Eigen3Config.cmake"
    - from: "CMakeLists.txt find_package(Boost CONFIG REQUIRED)"
      to: "/usr/lib/x86_64-linux-gnu/cmake/Boost-*/"
      pattern: "BoostConfig.cmake"
    - from: "CMakeLists.txt find_package(OpenCV REQUIRED)"
      to: "/usr/lib/x86_64-linux-gnu/cmake/opencv4/"
      pattern: "OpenCVConfig.cmake"
    - from: "CMakeLists.txt pkg_check_modules(ARAVIS)"
      to: "/usr/lib/x86_64-linux-gnu/pkgconfig/aravis-0.8.pc"
      pattern: "aravis-0.8.pc"

---

<objective>
Install all build tools and system dependencies required for the TGU_Robocore_2027 project to compile.

Purpose: The development machine (WSL2 Ubuntu) lacks cmake, g++, and essential development libraries. These must be installed before the project can be configured or built.

Output: Working build environment with cmake, g++, and all dependencies (Eigen3, Boost, OpenCV, Aravis, libusb) available for CMake find_package and compilation.
</objective>

<execution_context>
@/home/eldwen/.claude/get-shit-done/workflows/execute-plan.md
@/home/eldwen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/eldwen/TGU_Robocore_2027/.planning/ROADMAP.md
@/home/eldwen/TGU_Robocore_2027/.planning/STATE.md

# Reference: dependencies listed in project stack analysis
@/home/eldwen/TGU_Robocore_2027/.planning/codebase/STACK.md

# The top-level CMakeLists.txt shows which find_package calls need system packages
@/home/eldwen/TGU_Robocore_2027/CMakeLists.txt

# Research confirmed no fmt library needed -- Logger uses std::format
@/home/eldwen/TGU_Robocore_2027/.planning/phases/01-build_system/01-RESEARCH.md

# User decisions locking Eigen3 via apt (D-04)
@/home/eldwen/TGU_Robocore_2027/.planning/phases/01-build_system/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install build toolchain and all project system dependencies</name>
  <read_first>
    @/home/eldwen/TGU_Robocore_2027/CMakeLists.txt (lines 13-19: find_package calls)
    @/home/eldwen/TGU_Robocore_2027/.planning/phases/01-build_system/01-RESEARCH.md (section "Environment Availability" for package list)
  </read_first>
  <files>(system files only - no project files modified)</files>
  <action>
    Run the following commands to install all build tools and system dependencies:

    1. `sudo apt update`
    2. `sudo apt install -y cmake g++ build-essential`
    3. `sudo apt install -y libeigen3-dev libboost-all-dev libopencv-dev libaravis-dev libusb-1.0-0-dev`

    Note: Per D-04, Eigen3 is installed via apt (libeigen3-dev). The ROADMAP mentions "fmt 依赖" but the research confirmed Robocore uses std::format (C++20), not the external fmt library -- no fmt package needed. This is documented in RESEARCH.md section "fmt Library".

    The full command group:
    ```
    sudo apt update && sudo apt install -y cmake g++ build-essential libeigen3-dev libboost-all-dev libopencv-dev libaravis-dev libusb-1.0-0-dev
    ```

    Verify by checking each dependency with a single combined check:
    ```
    echo "cmake: $(cmake --version | head -1)" && \
    echo "g++: $(g++ --version | head -1)" && \
    echo "Eigen3: $(dpkg -l libeigen3-dev 2>/dev/null | grep -c '^ii')" && \
    echo "Eigen3 cmake config: $(ls /usr/lib/x86_64-linux-gnu/cmake/eigen3/Eigen3Config.cmake 2>/dev/null || echo NOT FOUND)" && \
    echo "Boost: $(dpkg -l libboost-all-dev 2>/dev/null | grep -c '^ii')" && \
    echo "OpenCV: $(pkg-config --modversion opencv4 2>/dev/null || echo NOT FOUND)" && \
    echo "Aravis: $(pkg-config --modversion aravis-0.8 2>/dev/null || echo NOT FOUND)"
    ```
  </action>
  <acceptance_criteria>
    cmake --version shows version >= 3.16, g++ --version shows version >= 10, libeigen3-dev installed with Eigen3Config.cmake available, libboost-all-dev installed, OpenCV and Aravis discoverable via pkg-config.
  </acceptance_criteria>
  <verify>
    <automated>
      cmake --version | head -1 && \
      g++ --version | head -1 && \
      dpkg -l libeigen3-dev 2>/dev/null | grep -q '^ii' && echo "Eigen3: installed" && \
      ls /usr/lib/x86_64-linux-gnu/cmake/eigen3/Eigen3Config.cmake 1>/dev/null 2>&1 && echo "Eigen3 cmake: found" && \
      pkg-config --modversion opencv4 2>/dev/null && \
      pkg-config --modversion aravis-0.8 2>/dev/null
    </automated>
  </verify>
  <done>
    All build tools and dependencies installed: cmake >= 3.16, g++, Eigen3 (with CMake config), Boost, OpenCV, Aravis, libusb.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| apt sources -> local system | System dependencies retrieved from Ubuntu package repositories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | apt package download | accept | Packages from Ubuntu official repos via TLS. Standard supply chain trust. No build-time code signing in this project. |
</threat_model>

<verification>
Run `cmake --version`, `g++ --version`, and dependency checks to confirm install.
</verification>

<success_criteria>
1. `cmake --version` outputs version >= 3.16
2. `g++ --version` outputs version >= 10
3. `dpkg -l libeigen3-dev` shows installed
4. `/usr/lib/x86_64-linux-gnu/cmake/eigen3/Eigen3Config.cmake` exists
5. `pkg-config --modversion opencv4` succeeds
6. `pkg-config --modversion aravis-0.8` succeeds
7. `dpkg -l libboost-all-dev` shows installed
</success_criteria>

<output>
After completion, create `.planning/phases/01-build_system/01-01-SUMMARY.md`
</output>
