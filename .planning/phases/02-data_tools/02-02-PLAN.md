---
phase: 02-data_tools
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/math_tools.hpp
  - tools/math_tools.cpp
  - tools/img_tools.hpp
  - tools/img_tools.cpp
autonomous: true
requirements:
  - MATH-01
  - MATH-02
  - MATH-03
  - MATH-04
  - MATH-05
  - MATH-06
  - MATH-07
  - IMG-01
  - IMG-02
  - IMG-03

must_haves:
  truths:
    - "tools::limit_rad(double) normalizes angles to (-pi, pi] range"
    - "tools::eulers() converts Quaterniond and Matrix3d to Euler angles with configurable axis order"
    - "tools::xyz2ypd() and tools::ypd2xyz() convert between Cartesian and spherical coordinates, with Jacobian matrices"
    - "tools::delta_time() measures elapsed time via std::chrono::steady_clock"
    - "tools::rotation_matrix() converts ypr Euler angles to 3x3 rotation matrix"
    - "tools::get_abs_angle() returns angle [0, pi] between two 2D vectors"
    - "tools::draw_point(), tools::draw_points(), tools::draw_text() draw on cv::Mat images"
    - "All functions are in namespace tools, accessible via #include \"tools/math_tools.hpp\" and #include \"tools/img_tools.hpp\""
  artifacts:
    - path: "tools/math_tools.hpp"
      provides: "Math utility function declarations with Eigen3 and chrono dependencies"
      min_lines: 55
    - path: "tools/math_tools.cpp"
      provides: "Math utility function implementations (11 functions + 1 template)"
      min_lines: 195
    - path: "tools/img_tools.hpp"
      provides: "Image drawing function declarations with OpenCV dependencies"
      min_lines: 25
    - path: "tools/img_tools.cpp"
      provides: "Image drawing function implementations (4 functions)"
      min_lines: 30
  key_links:
    - from: "tools/math_tools.hpp"
      to: "<Eigen/Geometry>"
      via: "#include <Eigen/Geometry>"
      pattern: '#include <Eigen/Geometry>'
    - from: "tools/math_tools.cpp"
      to: "tools/math_tools.hpp"
      via: "#include \"tools/math_tools.hpp\""
      pattern: 'include.*tools/math_tools.hpp'
    - from: "tools/img_tools.cpp"
      to: "tools/img_tools.hpp"
      via: "#include \"tools/img_tools.hpp\""
      pattern: 'include.*tools/img_tools.hpp'
    - from: "tools/math_tools.cpp"
      to: "<opencv2/core.hpp>"
      via: "#include <opencv2/core.hpp>  // CV_PI"
      pattern: '#include <opencv2/core.hpp>'

phase_checklist:
  - "All 4 files have TGU_ROBOCORE_2027_*_HPP include guard + #pragma once"
  - "All 4 files use correct namespace (tools per D-12/D-13)"
  - "Both .cpp files use full include paths (D-14)"
  - "Both .cpp files define MODULE (D-09)"
  - "math_tools.hpp/.cpp kept as one file (D-04)"
  - "No spdlog/fmt references in any file"
---

<objective>
创建 tools/math_tools.hpp/.cpp 和 tools/img_tools.hpp/.cpp，将 sp_vision_25 的数学工具和图像绘制函数迁移至 Robocore。

Purpose: 提供自瞄流水线所需的数学工具（角度限制、坐标转换、旋转矩阵、时间差、向量计算）和 OpenCV 绘制辅助函数。
Output: tools/math_tools.hpp/.cpp + tools/img_tools.hpp/.cpp
</objective>

<execution_context>
@/home/eldwen/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-data_tools/02-CONTEXT.md
@.planning/phases/02-data_tools/02-RESEARCH.md

<interfaces>
Existing Robocore tools namespace pattern (from tools/logger.hpp):
```cpp
#ifndef TGU_ROBOCORE_2027_TOOLS_LOGGER_HPP
#define TGU_ROBOCORE_2027_TOOLS_LOGGER_HPP
#pragma once

namespace tools {
// ...
}  // namespace tools

#endif
```

Target include guards for this plan:
- `TGU_ROBOCORE_2027_TOOLS_MATH_TOOLS_HPP`
- `TGU_ROBOCORE_2027_TOOLS_IMG_TOOLS_HPP`

Existing tools CMake includes:
```
${PROJECT_SOURCE_DIR}  →  resolves #include "tools/math_tools.hpp"
```

Key conventions:
- Full include paths: `#include "tools/math_tools.hpp"`
- MODULE define: `static constexpr const char* MODULE = "MATH_TOOLS";`
- No spdlog/fmt (per D-10, D-18)
</interfaces>

Source files for reference:
- Source header: /home/eldwen/sp_vision_25/tools/math_tools.hpp
- Source source: /home/eldwen/sp_vision_25/tools/math_tools.cpp
- Source header: /home/eldwen/sp_vision_25/tools/img_tools.hpp
- Source source: /home/eldwen/sp_vision_25/tools/img_tools.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tools/math_tools.hpp and tools/math_tools.cpp</name>
  <files>
    tools/math_tools.hpp
    tools/math_tools.cpp
  </files>
  <action>
    Create both files by copying from sp_vision_25 source with these adaptations:

    **tools/math_tools.hpp adaptations:**
    - Include guard: `TGU_ROBOCORE_2027_TOOLS_MATH_TOOLS_HPP` (replace `TOOLS__MATH_TOOLS_HPP`)
    - Add `#pragma once` after `#define`
    - Keep existing includes: `<Eigen/Geometry>`, `<chrono>`
    - Namespace: `namespace tools` (D-12) — same as source, no change needed
    - All function declarations preserved verbatim:
      - `double limit_rad(double)`
      - `Eigen::Vector3d eulers(Quaterniond, int axis0, int axis1, int axis2, bool extrinsic=false)` (2 overloads)
      - `Eigen::Matrix3d rotation_matrix(const Eigen::Vector3d& ypr)`
      - `Eigen::Vector3d xyz2ypd(const Eigen::Vector3d& xyz)`
      - `Eigen::MatrixXd xyz2ypd_jacobian(const Eigen::Vector3d& xyz)`
      - `Eigen::Vector3d ypd2xyz(const Eigen::Vector3d& ypd)`
      - `Eigen::MatrixXd ypd2xyz_jacobian(const Eigen::Vector3d& ypd)`
      - `double delta_time(const std::chrono::steady_clock::time_point&, const std::chrono::steady_clock::time_point&)`
      - `double get_abs_angle(const Eigen::Vector2d&, const Eigen::Vector2d&)`
      - `T square(T const&)` (template)
      - `double limit_min_max(double, double, double)`

    **tools/math_tools.cpp adaptations:**
    - Include path: `#include "math_tools.hpp"` → `#include "tools/math_tools.hpp"` (per D-14)
    - Add MODULE define: `static constexpr const char* MODULE = "MATH_TOOLS";` (per D-09)
    - Keep existing: `<cmath>`, `<opencv2/core.hpp>`
    - Namespace: `namespace tools` (D-12) — same as source
    - All 11 function implementations + 1 template preserved verbatim:
      - `limit_rad` (CV_PI angle wrapping)
      - `eulers` x2 (quaternion + rotation matrix → Euler angles)
      - `rotation_matrix` (ypr → 3x3 matrix)
      - `xyz2ypd` + `xyz2ypd_jacobian`
      - `ypd2xyz` + `ypd2xyz_jacobian`
      - `delta_time` (std::chrono)
      - `get_abs_angle` (vector dot product angle)
      - `square` (template — same file per D-04)
      - `limit_min_max` (clamp function)
    - Include order per CONVENTIONS.md:
      1. `#include "tools/math_tools.hpp"` (own header first)
      2. `<cmath>` (C++ standard)
      3. `<opencv2/core.hpp>` (third-party)

    Do NOT change:
    - Any algorithm logic, CV_PI usage, Eigen3 types, or numeric thresholds
    - Do NOT add spdlog/fmt/LOG_XXX (Phase 2 source has no logging)
    - Do NOT split math_tools into multiple files (D-04)
  </action>
  <verify>
    <automated>
      grep -c 'TGU_ROBOCORE_2027_TOOLS_MATH_TOOLS_HPP' tools/math_tools.hpp && \
      grep -c '#pragma once' tools/math_tools.hpp && \
      grep -c '#include "tools/math_tools.hpp"' tools/math_tools.cpp && \
      grep -c 'MODULE' tools/math_tools.cpp && \
      grep -c 'limit_rad' tools/math_tools.hpp && \
      grep -c 'eulers' tools/math_tools.hpp && \
      grep -c 'xyz2ypd_jacobian' tools/math_tools.hpp && \
      grep -c 'get_abs_angle' tools/math_tools.hpp && \
      grep -c 'namespace tools' tools/math_tools.hpp && \
      grep -c 'namespace tools' tools/math_tools.cpp && \
      grep -v '^\s*//' tools/math_tools.hpp | grep -c 'TOOLS__MATH_TOOLS_HPP' | xargs test 0 -eq
    </automated>
  </verify>
  <done>
    - Both files created with TGU_ROBOCORE_2027_TOOLS_MATH_TOOLS_HPP guard + #pragma once
    - namespace is tools in both files
    - math_tools.cpp uses full include path
    - MODULE define present
    - All 11 functions + 1 template present in header declarations
    - No spdlog/fmt/old guard references
    - Single file (not split per D-04)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tools/img_tools.hpp and tools/img_tools.cpp</name>
  <files>
    tools/img_tools.hpp
    tools/img_tools.cpp
  </files>
  <action>
    Create both files by copying from sp_vision_25 source with these adaptations:

    **tools/img_tools.hpp adaptations:**
    - Include guard: `TGU_ROBOCORE_2027_TOOLS_IMG_TOOLS_HPP` (replace `TOOLS__IMG_TOOLS_HPP`)
    - Add `#pragma once` after `#define`
    - Keep existing includes: `<opencv2/opencv.hpp>`, `<string>`, `<vector>`
    - Namespace: `namespace tools` (D-13) — same as source, no change needed
    - All function declarations preserved verbatim:
      - `void draw_point(cv::Mat&, const cv::Point&, const cv::Scalar& color={0,0,255}, int radius=3)`
      - `void draw_points(cv::Mat&, const std::vector<cv::Point>&, const cv::Scalar& color={0,0,255}, int thickness=2)`
      - `void draw_points(cv::Mat&, const std::vector<cv::Point2f>&, const cv::Scalar& color={0,0,255}, int thickness=2)` (overload)
      - `void draw_text(cv::Mat&, const std::string&, const cv::Point&, const cv::Scalar& color={0,255,255}, double font_scale=1.0, int thickness=2)`

    **tools/img_tools.cpp adaptations:**
    - Include path: `#include "img_tools.hpp"` → `#include "tools/img_tools.hpp"` (per D-14)
    - Add MODULE define: `static constexpr const char* MODULE = "IMG_TOOLS";` (per D-09)
    - No additional includes needed (source had none beyond its header)
    - Namespace: `namespace tools` (D-13) — same as source
    - All 4 function implementations preserved verbatim:
      - `draw_point` (calls cv::circle)
      - `draw_points` for `std::vector<cv::Point>` (calls cv::drawContours)
      - `draw_points` for `std::vector<cv::Point2f>` (converts to int_points, calls the other overload)
      - `draw_text` (calls cv::putText with FONT_HERSHEY_SIMPLEX)

    Do NOT change:
    - Any drawing parameters, OpenCV API calls, or default argument values
    - Do NOT add spdlog/fmt/LOG_XXX (Phase 2 source has no logging)
  </action>
  <verify>
    <automated>
      grep -c 'TGU_ROBOCORE_2027_TOOLS_IMG_TOOLS_HPP' tools/img_tools.hpp && \
      grep -c '#pragma once' tools/img_tools.hpp && \
      grep -c '#include "tools/img_tools.hpp"' tools/img_tools.cpp && \
      grep -c 'MODULE' tools/img_tools.cpp && \
      grep -c 'draw_point' tools/img_tools.hpp && \
      grep -c 'draw_text' tools/img_tools.hpp && \
      grep -c 'drawContours' tools/img_tools.cpp && \
      grep -c 'putText' tools/img_tools.cpp && \
      grep -c 'namespace tools' tools/img_tools.hpp && \
      grep -c 'namespace tools' tools/img_tools.cpp && \
      grep -v '^\s*//' tools/img_tools.hpp | grep -c 'TOOLS__IMG_TOOLS_HPP' | xargs test 0 -eq
    </automated>
  </verify>
  <done>
    - Both files created with TGU_ROBOCORE_2027_TOOLS_IMG_TOOLS_HPP guard + #pragma once
    - namespace is tools in both files
    - img_tools.cpp uses full include path
    - MODULE define present
    - All 4 drawing functions present with correct signatures
    - No spdlog/fmt/old guard references
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries exist in this phase. math_tools are pure mathematical functions operating on caller-provided double/Eigen values. img_tools draw on caller-provided cv::Mat references. No user input, network, or file I/O.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | math_tools functions | accept | Functions accept raw double/Eigen values. Invalid inputs (NaN, Inf) produce defined floating-point behavior. All callers are internal auto_aim modules — no cross-trust-boundary exposure. |
| T-02-02-02 | Denial of Service | img_tools draw functions | accept | cv::circle/cv::drawContours on unbounded coordinates could draw off-screen. Caller provides bounded coordinates. Not exposed to external input. |
</threat_model>

<verification>
1. `grep` gates pass for all required patterns (guard, namespace, includes, functions)
2. math_tools.hpp declares all 11 functions (+ 1 template) matching source signatures
3. img_tools.hpp declares all 4 drawing functions matching source signatures
4. `cmake --build build` will be verified in Plan 03
</verification>

<success_criteria>
1. math_tools.hpp has guard `TGU_ROBOCORE_2027_TOOLS_MATH_TOOLS_HPP` + `#pragma once`
2. img_tools.hpp has guard `TGU_ROBOCORE_2027_TOOLS_IMG_TOOLS_HPP` + `#pragma once`
3. All files use `namespace tools`
4. Both .cpp files use full include paths (D-14)
5. Both .cpp files define MODULE (D-09)
6. math_tools is one file (D-04)
7. All 11 math functions + 4 drawing functions migrated with correct signatures
8. Zero spdlog/fmt references
</success_criteria>

<output>
After completion, verify files are created with the Write tool. Plan 03 will handle CMake integration and build verification.
</output>
