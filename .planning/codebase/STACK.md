# Technology Stack

**Analysis Date:** 2026-05-01

## Languages

**Primary:**
- C++20 - All application, I/O, tools, and task code. Standard set via `set(CMAKE_CXX_STANDARD 20)` in `/home/eldwen/TGU_Robocore_2027/CMakeLists.txt`. Compiler extensions disabled (`CMAKE_CXX_EXTENSIONS OFF`).

**Secondary:**
- Bash - Build and utility scripts (`/home/eldwen/TGU_Robocore_2027/get_lines.sh`)

## Runtime

**Environment:**
- Ubuntu 22.04 (per README install instructions using `apt`)
- Target platform: x86_64 and aarch64 (NUC/robotics-grade computers for RM competition)

**Package Manager:**
- apt (system-level dependencies via `sudo apt install`)
- No language-level package manager (vcpkg/conan not used)

**Lockfile:**
- Not applicable (native C++ project, no language package manager)

## Frameworks

**Core:**
- No overarching web/API framework. The project is a native C++ robotics vision framework with modular library design.

**Testing:**
- No formal test framework (e.g., Google Test, Catch2). Test programs are standalone executables in `task/test/` that use `assert` or manual verification.

**Build/Dev:**
- CMake 3.16+ (`/home/eldwen/TGU_Robocore_2027/CMakeLists.txt`)
- GNU Make (generated by CMake)

## Key Dependencies

**Critical (required to compile):**
- Boost (specifically Boost.Asio for serial port communication) — `find_package(Boost CONFIG REQUIRED)` at `/home/eldwen/TGU_Robocore_2027/CMakeLists.txt:13`
- OpenCV (image capture, processing, display) — `find_package(OpenCV REQUIRED)` at `/home/eldwen/TGU_Robocore_2027/CMakeLists.txt:14`
- Aravis 0.8 (GStreamer-based industrial camera SDK, used for HikRobot cameras via GenICam) — `pkg_check_modules(ARAVIS REQUIRED aravis-0.8)` at `/home/eldwen/TGU_Robocore_2027/CMakeLists.txt:17`
- libusb (USB device access for HikRobot cameras) — listed in README install at `/home/eldwen/TGU_Robocore_2027/README.md:15`
- pthread (multithreading) — linked at `/home/eldwen/TGU_Robocore_2027/io/CMakeLists.txt:20`

**Vendored/Header-only:**
- toml++ (single-header TOML parser) — `/home/eldwen/TGU_Robocore_2027/tools/tomlpp.hpp`
- Foxglove SDK (WebSocket server for robotics visualization, vendored as prebuilt `.so`/`.a` + includes) — `/home/eldwen/TGU_Robocore_2027/tools/foxglove/`

**Planned (commented out):**
- Eigen3 (linear algebra) — commented out at `/home/eldwen/TGU_Robocore_2027/CMakeLists.txt:19`, planned for auto_aim migration
- Libraries from sp_vision_25 being migrated (see comparison below)

## Source Project Comparison (sp_vision_25)

The project at `/home/eldwen/sp_vision_25/` is the source being migrated FROM. Key stack differences:

| Aspect | TGU_Robocore_2027 (target) | sp_vision_25 (source) |
|--------|---------------------------|----------------------|
| C++ Standard | C++20 | C++17 |
| Build type | Debug | Release |
| Config format | TOML | YAML |
| Config parser | toml++ (vendored) | yaml-cpp (system) |
| Formatting | std::format (C++20 built-in) | fmt library (external) |
| Logging | Custom Logger with std::format | spdlog |
| Serial comm | Custom with Boost.Asio | wjwwood/serial library |
| CAN bus | Not yet implemented | SocketCAN |
| Camera SDK | Aravis (GStreamer/GenICam) | HikRobot SDK + MindVision SDK + V4L2 |
| ML inference | None planned for v1 | OpenVINO 2024.6 |
| Linear algebra | Planned: Eigen3 | Eigen3 |
| MPC/Planning | Not planned for v1 | TinyMPC (vendored) |
| Optimization | Not planned for v1 | Ceres Solver |
| JSON parsing | None | nlohmann_json |
| ROS2 | None | Optional (rclcpp + sp_msgs) |
| Visualization | Foxglove WebSocket (vendored SDK) | PlotJuggler (external tool) |
| Third-party SDKs | Vendored in tools/ | Mixed: system + vendored |

## Configuration

**Environment:**
- No environment variables used. All configuration is file-based.
- Camera USB udev rules defined in `/home/eldwen/TGU_Robocore_2027/README.md` (HikRobot vid:pid `2bdf:0001`)
- Serial device path hardcoded in tests (e.g., `/dev/ttyACM0`), intended to be configurable

**Build:**
- `/home/eldwen/TGU_Robocore_2027/CMakeLists.txt` — Top-level build configuration
- `/home/eldwen/TGU_Robocore_2027/io/CMakeLists.txt` — I/O static library
- `/home/eldwen/TGU_Robocore_2027/task/CMakeLists.txt` — Test and task executables
- `/home/eldwen/TGU_Robocore_2027/app/CMakeLists.txt` — Application algorithms (empty/placeholder, currently commented out in top-level)
- **Missing:** `/home/eldwen/TGU_Robocore_2027/tools/CMakeLists.txt` — Tools directory has no build target, despite being listed in `add_subdirectory(tools)` at `/home/eldwen/TGU_Robocore_2027/CMakeLists.txt:28`. This will cause a CMake error.

**Config files:**
- `/home/eldwen/TGU_Robocore_2027/config/testconfig.toml` — Single TOML config with game (enemy_color) and camera (name, exposure, gain, vid_pid, camera_matrix, distort_coeffs) sections
- Format: TOML, parsed with toml++ via `toml::parse_file()`

## Platform Requirements

**Development:**
- Ubuntu 22.04 (or Debian-based Linux)
- CMake >= 3.16
- C++20-compatible compiler (GCC 11+ or Clang 14+)
- Packages: `libboost-all-dev`, `libopencv-dev`, `libaravis-dev`, `aravis-tools`, `libusb-1.0-0-dev`

**Production:**
- RM competition robotics computer (NUC or similar x86_64)
- HikRobot industrial camera (USB3 Vision, vid:pid `2bdf:0001`)
- RoboMaster C-board or similar STM32-based MCU (serial communication)

---

*Stack analysis: 2026-05-01*
